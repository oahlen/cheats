% Utility commands

# Run dos2unix over a directory recursively
fd . --type f --print0 | xargs -0 dos2unix

# Find your external IP address
curl ifconfig.co

# Get the current weather forecast in the terminal
curl wttr.in
